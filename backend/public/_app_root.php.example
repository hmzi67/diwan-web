<?php
// OPTIONAL OVERRIDE — not generated by CI, and normally not needed.
//
// api/_bootstrap.php discovers the application root automatically by walking
// up from its own __DIR__. Only create a real _app_root.php here if your host
// uses a layout the candidate list does not match.
//
// Must be the absolute filesystem path to the directory CONTAINING src/ and
// config/ — not an FTP path. '/' is rejected as a bad value.
return '/home/YOURUSER/domains/example.com';
